import PhasePending from "@/components/PhasePending"
export default function Page() {
  return <PhasePending title="Patches" phaseLabel="Phase 4" description="By-client patch coverage matrix and rollout queue. Canary → wave → full with halt-on-failure." />
}
